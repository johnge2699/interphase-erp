{
  "name": "workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "tw-animate-css": "^1.4.0"
  }
}